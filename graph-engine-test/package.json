{
    "name": "test",
    "version": "0.0.0",
    "scripts": {
        "start": "nodemon --verbose",
        "wipe": "kill -9 $(lsof -t -i:5000)"
    },
    "dependencies": {
        "concurrently": "^5.3.0",
        "express": "^4.18.2",
        "socket.io": "^3.0.3"
    }
}
